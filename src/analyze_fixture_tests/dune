(include_subdirs unqualified)

(library
  (name AnalyzeFixtureTests)
  (libraries Belt str Core Util Analyze rely.lib TestFramework Relite)
  (flags :standard -open Analyze -open Util -w -9)
  (preprocess (pps Belt_ppx Ppx_monads))
  )
