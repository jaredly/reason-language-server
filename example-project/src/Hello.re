
let x = 10;

/** Some docs about this awesome thing. */
let awesome = 100;
let thing = "thing";

let transform = (x, y) => x ++ string_of_float(y);

let z = transform("hello ", 5.);

let zzz = 1;
let more = 20;
/** Something here */
let added = 10;
open Other;
Inner.x